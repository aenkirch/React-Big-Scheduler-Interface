{
  "name": "React-Big-Scheduler-Interface",
  "version": "0.1.0",
  "description": "You're on the server side of the project",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "mysql": "^2.16.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
